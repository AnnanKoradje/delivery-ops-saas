<x-portal-layout>
    <div><p class="text-xs font-semibold uppercase tracking-[.16em] text-blue-600">Account profile</p><h1 class="mt-1 text-2xl font-semibold tracking-tight">Your contact details</h1><p class="mt-1 text-sm text-slate-500">Keep your delivery contact details current.</p></div>
    <form method="POST" action="{{ route('portal.profile.update') }}" class="mt-6 max-w-2xl rounded-2xl border border-slate-200 bg-white p-6 shadow-sm">@csrf @method('PATCH')<div class="grid gap-5 sm:grid-cols-2"><div class="sm:col-span-2"><label class="text-sm font-semibold">Name</label><input name="name" value="{{ old('name', $customer->name) }}" required class="mt-2 w-full rounded-xl border-slate-300 focus:border-blue-500 focus:ring-blue-500">@error('name')<p class="mt-2 text-sm text-rose-600">{{ $message }}</p>@enderror</div><div><label class="text-sm font-semibold">Email</label><input value="{{ auth('customer')->user()->email }}" disabled class="mt-2 w-full rounded-xl border-slate-200 bg-slate-50 text-slate-500"></div><div><label class="text-sm font-semibold">Phone</label><input name="phone" value="{{ old('phone', $customer->phone) }}" class="mt-2 w-full rounded-xl border-slate-300 focus:border-blue-500 focus:ring-blue-500"></div><div class="sm:col-span-2"><label class="text-sm font-semibold">Address</label><textarea name="address" rows="4" class="mt-2 w-full rounded-xl border-slate-300 focus:border-blue-500 focus:ring-blue-500">{{ old('address', $customer->address) }}</textarea></div></div><button class="mt-6 rounded-xl bg-blue-600 px-5 py-3 text-sm font-semibold text-white">Save profile</button>@if (session('status'))<p class="mt-5 rounded-xl bg-emerald-50 p-4 text-sm font-semibold text-emerald-700">{{ session('status') }}</p>@endif</form>
</x-portal-layout>
