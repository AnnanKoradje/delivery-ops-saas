<x-portal-layout>
    <div class="flex flex-wrap items-end justify-between gap-4"><div><p class="text-xs font-semibold uppercase tracking-[.16em] text-blue-600">Delivery tracking</p><h1 class="mt-1 text-2xl font-semibold tracking-tight">{{ $delivery->reference }}</h1><p class="mt-1 text-sm text-slate-500">Status: {{ $statuses[$delivery->status] }}</p></div><a href="{{ route('portal.deliveries.index') }}" class="rounded-xl border border-slate-300 px-4 py-3 text-sm font-semibold text-slate-700">All deliveries</a></div>
    <div class="mt-6 grid gap-6 lg:grid-cols-[.9fr_1.1fr]"><section class="rounded-2xl border border-slate-200 bg-white p-6 shadow-sm"><h2 class="font-semibold">Delivery details</h2><dl class="mt-5 space-y-4"><div><dt class="text-xs font-semibold uppercase tracking-[.14em] text-slate-400">Drop-off</dt><dd class="mt-2 whitespace-pre-line text-sm leading-6 text-slate-700">{{ $delivery->dropoff_address }}</dd></div><div><dt class="text-xs font-semibold uppercase tracking-[.14em] text-slate-400">Scheduled time</dt><dd class="mt-2 text-sm text-slate-700">{{ $delivery->scheduled_at?->format('M j, Y g:i A') ?: 'To be confirmed' }}</dd></div></dl></section><section class="rounded-2xl border border-slate-200 bg-white p-6 shadow-sm"><h2 class="font-semibold">Delivery history</h2><ol class="mt-5 space-y-4">@forelse ($delivery->statusHistory as $history)<li class="border-l-2 border-blue-200 pl-4"><p class="text-sm font-semibold text-slate-800">{{ $statuses[$history->to_status] }}</p><p class="mt-1 text-xs text-slate-500">{{ $history->created_at->format('M j, Y g:i A') }}</p></li>@empty<li class="text-sm text-slate-500">Status updates will appear here.</li>@endforelse</ol></section></div>
</x-portal-layout>
