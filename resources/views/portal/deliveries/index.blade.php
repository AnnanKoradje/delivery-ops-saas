<x-portal-layout>
    <div class="flex flex-wrap items-end justify-between gap-4"><div><p class="text-xs font-semibold uppercase tracking-[.16em] text-blue-600">Delivery tracking</p><h1 class="mt-1 text-2xl font-semibold tracking-tight">Your deliveries</h1><p class="mt-1 text-sm text-slate-500">Only delivery records linked to your customer account are shown.</p></div><a href="{{ route('portal.profile.edit') }}" class="rounded-xl border border-slate-300 px-4 py-3 text-sm font-semibold text-slate-700">Update profile</a></div>
    <section class="mt-6 overflow-hidden rounded-2xl border border-slate-200 bg-white shadow-sm"><div class="divide-y divide-slate-100">@forelse ($deliveries as $delivery)<a href="{{ route('portal.deliveries.show', $delivery) }}" class="block px-6 py-5 transition hover:bg-slate-50"><div class="flex flex-wrap items-center justify-between gap-3"><div><p class="text-sm font-semibold text-slate-900">{{ $delivery->reference }}</p><p class="mt-1 text-xs text-slate-500">Scheduled {{ $delivery->scheduled_at?->format('M j, Y g:i A') ?: 'to be confirmed' }}</p></div><span class="rounded-full bg-blue-50 px-2.5 py-1 text-xs font-semibold text-blue-700">{{ $statuses[$delivery->status] }}</span></div></a>@empty<div class="px-6 py-16 text-center"><p class="font-semibold text-slate-900">No deliveries are linked to this account</p><p class="mt-2 text-sm text-slate-500">Contact your delivery company if you expected to see an order here.</p></div>@endforelse</div>@if ($deliveries->hasPages())<div class="border-t border-slate-100 px-6 py-4">{{ $deliveries->links() }}</div>@endif</section>
    @if (session('status'))<p class="mt-5 rounded-xl bg-emerald-50 p-4 text-sm font-semibold text-emerald-700">{{ session('status') }}</p>@endif
</x-portal-layout>
