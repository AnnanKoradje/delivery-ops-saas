<x-app-layout>
    <x-slot name="header"><div><p class="text-xs font-semibold uppercase tracking-[.16em] text-blue-600">Platform onboarding</p><h1 class="mt-1 text-2xl font-semibold tracking-tight">Company applications</h1></div></x-slot>
    <div class="space-y-6"><div class="flex flex-wrap items-center justify-between gap-3"><div class="flex flex-wrap gap-2">@foreach (['' => 'All', 'pending' => 'Pending', 'active' => 'Active', 'suspended' => 'Suspended', 'rejected' => 'Rejected'] as $value => $label)<a href="{{ route('platform.companies.index', $value === '' ? [] : ['status' => $value]) }}" class="rounded-full px-3 py-2 text-sm font-semibold {{ $status === $value ? 'bg-slate-950 text-white' : 'bg-white text-slate-600 ring-1 ring-slate-200 hover:bg-slate-50' }}">{{ $label }}</a>@endforeach</div><a href="{{ route('platform.companies.create') }}" class="rounded-xl bg-slate-950 px-4 py-2.5 text-sm font-semibold text-white hover:bg-slate-800">Create company</a></div><section class="overflow-hidden rounded-2xl border border-slate-200 bg-white shadow-sm"><div class="divide-y divide-slate-100">@forelse($companies as $company)<a href="{{ route('platform.companies.show', $company) }}" class="flex items-center justify-between gap-4 px-6 py-5 transition hover:bg-slate-50"><div class="min-w-0"><p class="truncate font-semibold text-slate-900">{{ $company->name }}</p><p class="mt-1 truncate text-sm text-slate-500">{{ $company->contact_email }} · {{ $company->created_at->format('M j, Y') }}</p></div><span class="rounded-full px-2.5 py-1 text-xs font-semibold {{ $company->status === 'active' ? 'bg-emerald-50 text-emerald-700' : ($company->status === 'suspended' ? 'bg-rose-50 text-rose-700' : ($company->status === 'rejected' ? 'bg-slate-100 text-slate-600' : 'bg-amber-50 text-amber-700')) }}">{{ ucfirst($company->status) }}</span></a>@empty<div class="px-6 py-14 text-center text-sm text-slate-500">No company applications match this filter.</div>@endforelse</div></section>{{ $companies->links() }}</div>
</x-app-layout>
